openapi: 3.0.0
info:
  title: Parser Test
  version: '1.0'
components:
  schemas:
    Parse:
      $ref: '#/components/schemas/ParseEnum'
    ParseEnum:
      title: Parse It
      description: Can it parse it?
      type: string
      enum:
        - Yes
        - No