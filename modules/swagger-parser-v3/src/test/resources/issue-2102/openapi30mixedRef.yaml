openapi: 3.0.0
paths:
  /mixed:
    get:
      parameters:
        - in: query
          name: myParam
          schema: { type: string }
        - $ref: '#/components/parameters/headerMyParam'
      responses:
        '200':
          description: OK
components:
  parameters:
    headerMyParam:
      in: header
      name: myParam
      schema: { type: string }
