openapi: 3.1.0
info:
  title: siblings JSONschema
  version: 1.0.0
servers:
  - url: /
paths: { }
components:
  schemas:
    profile:
      const: sales
      contentEncoding: base64
      contentMediaType: text/html
      contentSchema:
        type: string
      $ref: ./ex.json#user-profile