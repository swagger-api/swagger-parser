openapi: 3.1.0
info:
  title: siblings JSONschema
  version: 1.0.0
servers:
  - url: /
paths: { }
components:
  schemas:
    profile:
      description: siblings refs
      required:
        - login
        - password
      maxItems: 2
      $ref: ./ex.json#user-profile
      properties:
        login:
          type: string
        password:
          type: string