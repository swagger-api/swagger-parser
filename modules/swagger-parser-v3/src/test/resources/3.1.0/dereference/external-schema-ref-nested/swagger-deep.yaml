openapi: 3.1.0
info:
  title: Deep Nested Ref API
  version: 1.0.0

paths:
  /orders:
    get:
      operationId: getOrders
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                $ref: schemas/deep.yaml#/components/schemas/Order
