openapi: "3.0.0"
info:
  title: test data for nested list ext references
  version: '0.0.0'
paths:
  /:
    get:
      responses:
        "200":
          content:
            '*/*':
              schema:
                $ref: "./issue_286_PetList.yaml"