openapi: 3.0.0
info:
  title: test
  version: 1.0.0

components:
  schemas:
    Product:
      type: object
      properties:
        rootOnly:
          type: string
    GetProductResponse:
      $ref: './common/models/GetProductResponse.yaml'
