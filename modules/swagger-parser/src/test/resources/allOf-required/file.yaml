---
swagger: "2.0"
info:
  version: "2.0"
  title: "API"
basePath: "/api"
produces:
- "application/json"
paths:
  /:
    get:
      description: path
      responses:
        200:
          description: "ok"
          schema:
            $ref: "#/definitions/composed"
definitions:
  common:
    type: "object"
    properties:
      field:
        type: "string"
  composed:
    type: "object"
    required:
      - field
      - field2
    allOf:
      - $ref: "#/definitions/common"
      - type: "object"
        properties:
          field2:
            type: "string"
