swagger: '2.0'
definitions:
  Animal:
    required:
      - id
    properties:
      id:
        type: string
      categories:
        $ref: './components/entities.yaml#/definitions/Category'
  Inventory:
    properties:
      book:
        $ref: './book.yaml'
  Orders:
    properties:
      book:
        $ref: 'book.yaml'